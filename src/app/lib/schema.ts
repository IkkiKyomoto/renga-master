export type loginSchema = {
    email: string;
    password: string;
    };

export type registerSchema = {
    name: string;
    email: string;
    password: string;
    passwordConfirm: string;
    };
    